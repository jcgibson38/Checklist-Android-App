<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/list_element_height"
    android:background="@drawable/item_background">

    <ImageView
        android:id="@+id/list_item_colorSplotch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_margin="@dimen/activity_horizontal_margin"
        android:scaleType="center"
        android:background="@drawable/color_splotch_selector"/>

    <LinearLayout
        android:id="@+id/item_text_linearLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingLeft="@dimen/list_textView_padding_start"
        android:paddingStart="@dimen/list_textView_padding_start"
        android:paddingRight="@dimen/list_textView_padding_end"
        android:paddingEnd="@dimen/list_textView_padding_end"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:orientation="vertical">

        <TextView
            android:id="@+id/list_item_title_textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/primary_text_color"
            android:textSize="@dimen/text_size"
            tools:text="title"/>

        <TextView
            android:id="@+id/list_item_info_textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/secondary_text_color"
            android:textSize="@dimen/subtitle_text_size"
            tools:text="info"/>

    </LinearLayout>

    <CheckBox
        android:id="@+id/list_item_checkBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:paddingTop="@dimen/list_textView_padding_end"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingEnd="@dimen/activity_horizontal_margin"
        android:checked="true"
        android:focusable="false"/>

</RelativeLayout>